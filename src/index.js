import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import RouterConfig from './Router/Route'; //路由配置

ReactDOM.render(
  <RouterConfig/>,
  document.getElementById('root')
);
